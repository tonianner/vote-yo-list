app.controller("ToDoCtrl", ToDoCtrl)

ToDoCtrl.$inject = ['ToDoFactory', 'EventFactory','$scope', '$stateParams']

function ToDoCtrl(ToDoFactory,EventFactory, $scope, $stateParams) {

  $scope.selectedEventId = {}

  $scope.getId = function(id) {
    $scope.selectedEventId = EventFactory.get({id: id})
  }

  var ToDoList = ToDoFactory;
  // Index
  $scope.allTasks = ToDoList.get({eventId: $scope.eventId},
    function(){
      ToDoList.query({id: id})
    }
  )

}
