app.controller("DashBoardCtrl", DashBoardCtrl)

DashBoardCtrl.$inject = ['TaskFactory','EventFactory', '$auth', '$scope', '$stateParams']

function DashBoardCtrl(TaskFactory, EventFactory, $auth, $scope, $stateParams) {
  // $scope.user = $auth.user
  var EventList = EventFactory;
  var TaskList = TaskFactory;

  // Index
  $scope.allEvents = EventList.query();

  // $scope.allTasks = TaskFactory.query({eventId: $stateParams.id})

  // $scope.allTasks = TaskFactory.get({eventId: $stateParams.id, id: 38})

  $scope.selectEvent = function(id) {
    $scope.selectedEvent = EventList.get({id: id})
  };
}
