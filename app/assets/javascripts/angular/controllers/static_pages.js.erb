app.controller('homeCtrl', ['$scope', '$auth', '$location', 'UserFactory',
  function($scope, $auth, $location, UserFactory){

    // wow: animation start on scroll
    new WOW().init();

    console.log(UserFactory)
    $scope.current_user = UserFactory.identity

    $scope.logout = function () {
      $auth.signOut().then(function(resp) {
        // console.log(resp);
      }).catch(function(resp) {
        // console.log(resp);
      });
    };
}])
