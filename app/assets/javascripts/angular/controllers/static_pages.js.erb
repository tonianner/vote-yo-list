app.controller('homeCtrl', ['$scope', '$auth', '$location',
  function($scope, $auth, $location){

    // wow: animation start on scroll
    new WOW().init();

    $scope.logout = function () {
      $auth.signOut().then(function(resp) {
        console.log(resp);
      }).catch(function(resp) {
        console.log(resp);
      });
    };
}])
